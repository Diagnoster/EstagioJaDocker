import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Icones {
  static const usuario = Icon(Icons.account_circle_rounded);
  static const senha = Icon(Icons.lock_rounded);
  static const visualizar = Icon(Icons.remove_red_eye_rounded);
  static const home = Icon(Icons.home_outlined, size: 25);
  static const vagas = Icon(Icons.work_outline, size: 25);
  static const notificacoes = Icon(Icons.notifications_none_outlined, size: 25,);
  static const pesquisar = Icon(Icons.search, size: 30, color: Colors.blueGrey);
  static const link = Icon(Icons.open_in_new, color: Colors.black,);
}